# Task ID: 13
# Title: Extract Persistent GameLayout Component
# Status: done
# Dependencies: 3, 9
# Priority: medium
# Description: Create a persistent GameLayout component that wraps all phase screens with a consistent layout structure including phase tracker at top, control panel area at bottom left, and main content area.
# Details:
Create GameLayout.tsx component to provide consistent layout structure:
- Extract layout logic from main.tsx into dedicated GameLayout component
- Position phase tracker (Run #, Round #) at the top of the screen, persistent across all phases
- Reserve fixed area at bottom left for control panel (development tools, reset buttons)
- Provide main content area that dynamically renders current phase component
- Use CSS Grid or Flexbox for responsive layout structure
- Ensure layout remains stable during phase transitions

GameLayout component structure:
```tsx
interface GameLayoutProps {
  children: React.ReactNode;
  currentPhase: GamePhase;
  runNumber: number;
  roundNumber: number;
}

export const GameLayout: React.FC<GameLayoutProps> = ({
  children,
  currentPhase,
  runNumber,
  roundNumber
}) => {
  return (
    <div className="h-screen grid grid-rows-[auto_1fr_auto]">
      {/* Top: Phase Tracker */}
      <header className="bg-gray-800 text-white p-4">
        <div className="flex justify-between items-center">
          <h1>Game Title</h1>
          <div className="text-lg font-semibold">
            Run #{runNumber} - Round #{roundNumber}
          </div>
        </div>
      </header>
      
      {/* Main Content Area */}
      <main className="flex-1 overflow-auto p-4">
        {children}
      </main>
      
      {/* Bottom Left: Control Panel Area */}
      <footer className="bg-gray-100 p-4">
        <div className="flex justify-start">
          <div className="control-panel-area">
            {/* Reserved space for control panel */}
          </div>
        </div>
      </footer>
    </div>
  );
};
```

Integration steps:
- Update main.tsx to use GameLayout wrapper around phase components
- Pass phase information and game state to layout component
- Ensure all phase components render properly within the main content area
- Maintain existing functionality while improving layout consistency

# Test Strategy:
Verify GameLayout renders correctly with all phase components (Combat, Shop, Death, Lab), confirm phase tracker displays accurate run/round numbers and updates properly during transitions, test that control panel area is properly reserved and accessible, ensure layout remains stable during phase changes without flickering or repositioning, verify responsive behavior on different screen sizes, and test that all existing functionality continues to work within the new layout structure.

# Subtasks:
## 1. Create GameLayout Component with Phase Tracker [done]
### Dependencies: None
### Description: Create the GameLayout.tsx component with a persistent phase tracker at the top that displays run and round numbers across all game phases.
### Details:
Create src/components/GameLayout.tsx with the interface GameLayoutProps including children, currentPhase, runNumber, and roundNumber. Implement the component using CSS Grid with grid-rows-[auto_1fr_auto] for three-section layout. Add header section with game title on left and 'Run #{runNumber} - Round #{roundNumber}' on right. Use Tailwind classes: bg-gray-800 text-white p-4 for header styling, flex justify-between items-center for header layout, and text-lg font-semibold for phase tracker text.

## 2. Add Main Content Area to GameLayout [done]
### Dependencies: 13.1
### Description: Implement the main content area in GameLayout that will dynamically render the current phase component as children with proper overflow handling.
### Details:
Add main element to GameLayout with flex-1 overflow-auto p-4 classes to create scrollable content area that takes remaining vertical space. Render {children} prop within this main section. Ensure the content area properly handles different phase components and maintains consistent padding. The main area should expand to fill available space between header and footer.

## 3. Add Control Panel Area to GameLayout Footer [done]
### Dependencies: 13.2
### Description: Implement the footer section with a reserved fixed area at bottom left specifically for control panel components like development tools and reset buttons.
### Details:
Add footer element with bg-gray-100 p-4 classes. Inside footer, create div with flex justify-start layout containing a control-panel-area div. This area should be positioned at bottom left and maintain consistent spacing. Add placeholder comment indicating this space is reserved for control panel components. The footer should have fixed height and not interfere with main content scrolling.

## 4. Integrate GameLayout into App Component [done]
### Dependencies: 13.3
### Description: Update the main App component to use GameLayout wrapper around phase components, passing necessary game state and ensuring all existing functionality is preserved.
### Details:
Import GameLayout into App.tsx. Wrap the current phase rendering logic with GameLayout component, passing currentPhase, runNumber, and roundNumber as props. Move the phase component rendering (PhaseIntro, PhaseAction, PhaseResults, PhaseEnd) to be children of GameLayout. Remove any existing layout/header code from App.tsx that's now handled by GameLayout. Ensure game state management and phase transitions continue to work correctly.

