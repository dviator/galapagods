# Task ID: 3
# Title: Create Phase Tracker UI Component
# Status: pending
# Dependencies: 1, 2
# Priority: medium
# Description: Build a persistent phase tracker component that displays on the left side of the screen, showing the current phase and overall game loop progress.
# Details:
Create PhaseTracker.tsx component:
- Fixed position on the left side of the screen
- Visual indicators for each phase (combat, shop, death, lab)
- Highlight current active phase
- Show progression through the game loop
- Use Tailwind CSS for styling with modern, clean design

Component structure:
```tsx
interface PhaseTrackerProps {
  currentPhase: GamePhase;
  runNumber: number;
}

export const PhaseTracker: React.FC<PhaseTrackerProps> = ({currentPhase, runNumber}) => {
  const phases = ['combat', 'shop', 'death', 'lab'];
  return (
    <div className="fixed left-0 top-0 h-full w-48 bg-gray-800 text-white p-4">
      {/* phase indicators */}
    </div>
  );
};
```

# Test Strategy:
Verify the component renders correctly, updates when phase changes, maintains fixed position, and displays proper visual feedback for active phase.
