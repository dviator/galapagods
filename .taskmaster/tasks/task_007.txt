# Task ID: 7
# Title: Implement Lab Phase Placeholder Component
# Status: done
# Dependencies: 1, 2
# Priority: low
# Description: Create a placeholder Lab component that will eventually house evolution/breeding mechanics, currently displaying a coming soon message and allowing players to start a new run.
# Details:
Create Lab.tsx component:
- Display placeholder message: "Lab/Evolution phase coming soon"
- Show concept art or wireframes for future evolution system
- Implement "Start New Run" button that resets game state
- Reset upgrades, generate new team, reset run counter
- Prepare component structure for future evolution mechanics

Lab component:
```tsx
interface LabProps {
  onStartNewRun: () => void;
}

export const Lab: React.FC<LabProps> = ({onStartNewRun}) => {
  const handleNewRun = () => {
    // Reset game state
    onStartNewRun();
  };
  
  return (
    <div className="flex flex-col items-center justify-center h-full">
      <h1>Lab/Evolution Phase</h1>
      <p>Coming Soon...</p>
      <button onClick={handleNewRun}>Start New Run</button>
    </div>
  );
};
```

# Test Strategy:
Verify placeholder displays correctly, Start New Run button resets all game state properly, and component is structured to easily add evolution features later.
