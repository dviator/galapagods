# Task ID: 4
# Title: Refactor Combat System into Separate Module
# Status: done
# Dependencies: 1, 2
# Priority: high
# Description: Extract the existing combat simulator logic into a dedicated Combat component and supporting utility functions, maintaining all current functionality while making it modular.
# Details:
Create Combat.tsx component and combatUtils.ts:
- Move existing combat logic into Combat component
- Extract combat calculations into combatUtils.ts (damage calculation, turn order, etc.)
- Implement automatic transition to shop phase when all enemies defeated
- Keep Reset button functionality for development (resets current combat only)
- Maintain existing combat UI and interactions

Combat component structure:
```tsx
interface CombatProps {
  playerTeam: Character[];
  enemies: Enemy[];
  onCombatEnd: (result: 'victory' | 'defeat') => void;
  onReset: () => void;
}

export const Combat: React.FC<CombatProps> = (props) => {
  // combat logic implementation
};
```

# Test Strategy:
Ensure all existing combat functionality works identically, verify automatic phase transitions trigger correctly, and test that Reset button only affects current combat state.
