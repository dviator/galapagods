# Task ID: 8
# Title: Create Game State Management System
# Status: pending
# Dependencies: 1, 2
# Priority: high
# Description: Implement centralized game state management to handle player team, enemies, upgrades, run progress, and coordinate between all game phases.
# Details:
Create gameState.ts with React Context for global state:
- Manage player team, current enemies, purchased upgrades
- Track run statistics and progression
- Handle enemy difficulty scaling between rounds
- Implement state reset for new runs
- Provide actions for state updates from each phase

GameState implementation:
```typescript
interface GameStateContextType {
  gameState: GameState;
  updatePlayerTeam: (team: Character[]) => void;
  generateEnemies: (difficulty: number) => void;
  purchaseUpgrade: (upgrade: Upgrade) => void;
  resetForNewRun: () => void;
  incrementRound: () => void;
}

export const GameStateProvider: React.FC<{children: ReactNode}> = ({children}) => {
  const [gameState, setGameState] = useState<GameState>(initialState);
  // implementation
};
```

# Test Strategy:
Test that state updates correctly across all phases, enemy difficulty scales properly, upgrades persist between combats, and new run reset works completely.
