import type { TargetingRule } from '../../types';

export const frontTargetingRule: TargetingRule = {
  name: "Front",
  // getTargets: (attacker, battlefield) => { ... }
};
export const rightTargetingRule: TargetingRule = {
  name: "Right",
  // getTargets: (attacker, battlefield) => { ... }
};